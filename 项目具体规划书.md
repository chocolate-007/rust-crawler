# Rust课程期末大作业项目具体规划书

## 1. 项目名称

`RustCrawler`：基于 Rust 的多线程网页爬虫程序

## 2. 项目定位

本项目是一个可独立运行的命令行爬虫工具。用户输入一个或多个起始 URL 后，程序能够并发抓取网页内容，解析页面中的链接，按设定规则继续爬取，并将结果保存到本地。

项目目标不是做“大而全”的商业搜索引擎，而是完成一个结构完整、功能闭环、能体现 Rust 工程实践的课程级项目。

## 3. 选题理由

“多线程爬虫程序”非常适合作为 Rust 课程大作业，原因如下：

- 能自然体现并发编程能力
- 网络请求、HTML 解析、任务调度、结果存储都适合模块化设计
- 错误场景丰富，适合展示 `Result` 和错误传播
- 队列、去重、共享状态适合体现所有权、借用、`Arc`、`Mutex`
- 可以写出比较完整的测试和文档
- 项目难度适中，代码量容易控制在课程要求范围内

## 4. 项目目标

完成一个命令行多线程爬虫，具备以下能力：

- 从种子 URL 开始抓取网页
- 支持设置最大爬取深度
- 支持限制抓取页面总数
- 支持域名白名单，避免跨站无限扩散
- 支持多线程并发抓取
- 提取页面标题、链接、状态码等信息
- 将结果保存为 JSON 或 CSV
- 输出爬取统计信息

## 5. 核心功能规划

### 5.1 第一阶段必做功能

1. 命令行参数输入
   支持输入起始 URL、线程数、最大深度、最大页面数、输出文件路径等参数。

2. 网页抓取
   使用 HTTP 客户端发送请求并获取页面内容。

3. HTML 解析
   从 HTML 中提取标题和页面中的超链接。

4. 链接去重
   避免重复抓取同一页面。

5. 多线程任务调度
   多个线程并发消费待抓取链接队列。

6. 结果持久化
   将抓取结果保存到本地文件。

7. 运行统计
   输出已抓取页面数、失败页面数、发现链接数等信息。

### 5.2 第二阶段增强功能

- 支持超时与重试机制
- 支持 robots.txt 基础检查
- 支持 URL 规范化
- 支持关键词搜索页面内容
- 支持导出为多种格式
- 支持日志分级输出

### 5.3 可选加分功能

- 使用线程池优化并发效率
- 支持断点续爬
- 支持异步版本对比实现
- 支持简单网页内容摘要

## 6. 使用场景

项目可以用于以下演示场景：

- 抓取一个网站首页及若干层子页面
- 统计页面标题和内部链接结构
- 保存站点页面抓取结果用于后续分析

## 7. 技术路线

### 7.1 项目形态

- 项目类型：Rust 命令行应用
- 构建工具：`cargo`
- 运行方式：本地终端执行

### 7.2 建议依赖

建议使用以下依赖：

- `clap`
  用于命令行参数解析

- `reqwest`
  用于发送 HTTP 请求

- `scraper`
  用于解析 HTML 并提取链接和标题

- `serde`
  用于数据序列化和反序列化

- `serde_json`
  用于保存 JSON 结果

- `csv`
  用于导出 CSV

- `thiserror`
  用于统一错误定义

- `url`
  用于 URL 解析与拼接

- `log` 与 `env_logger`
  用于日志输出

可选依赖：

- `rayon`
  如果后期需要尝试并行处理部分计算逻辑

- `tokio`
  如果后续想增加异步版本作扩展对比

## 8. Rust 核心特性体现方案

### 8.1 所有权与借用

项目中会重点体现：

- 抓取结果的所有权归属
- 共享任务队列时的所有权管理
- 对配置和结果集合的借用传递
- 尽量避免不必要的字符串克隆

### 8.2 `struct` 使用

计划定义以下核心结构体：

- `CrawlerConfig`
  存储程序配置项

- `CrawlTask`
  表示一个待抓取任务

- `PageData`
  表示单个页面抓取结果

- `CrawlStats`
  表示运行统计信息

- `Crawler`
  表示爬虫调度器主体

### 8.3 `enum` 使用

计划使用以下枚举：

- `OutputFormat`
  表示导出格式，如 JSON、CSV

- `TaskStatus`
  表示抓取状态，如成功、失败、跳过

- `AppError`
  表示统一错误类型

### 8.4 `trait` 使用

计划设计以下 trait：

- `Storage`
  抽象不同结果存储方式

- `LinkExtractor`
  抽象链接提取行为

- `PageFilter`
  抽象页面过滤规则

这样可以增强项目的扩展性和可维护性。

### 8.5 泛型

泛型可以用于：

- 通用存储接口
- 测试辅助工具
- 部分可复用的数据处理函数

### 8.6 生命周期

如果在页面解析、引用切片或过滤器组合中有必要，会合理使用生命周期。生命周期应服务于真实设计需求，而不是为了展示语法强行加入。

## 9. 并发设计方案

这是本项目的重点亮点之一。

### 9.1 并发模型

建议采用“任务队列 + 多工作线程”的设计：

- 主线程负责初始化配置、投放种子链接、回收统计结果
- 多个工作线程从共享队列中取出任务
- 每个线程执行页面请求、HTML 解析、链接抽取
- 新发现且未访问过的链接继续加入队列

### 9.2 共享资源

共享资源包括：

- 待抓取任务队列
- 已访问 URL 集合
- 全局统计信息
- 最终抓取结果集合

### 9.3 并发原语

计划体现以下 Rust 并发能力：

- `thread`
- `Arc`
- `Mutex`
- `mpsc` channel 或共享队列

### 9.4 并发设计建议

为了降低实现风险，开发时采用“两步走”：

1. 先实现单线程正确版本
2. 再重构为多线程版本

这样更稳妥，也便于调试和测试。

## 10. 模块设计

建议至少划分为以下模块：

### 10.1 `cli`

职责：

- 定义命令行参数
- 解析用户输入

### 10.2 `config`

职责：

- 保存配置结构
- 校验配置参数

### 10.3 `crawler`

职责：

- 爬虫调度主逻辑
- 线程管理
- 任务分发

### 10.4 `fetcher`

职责：

- 发送 HTTP 请求
- 获取状态码和页面正文

### 10.5 `parser`

职责：

- 解析 HTML
- 提取页面标题
- 提取超链接

### 10.6 `storage`

职责：

- 保存结果到 JSON 或 CSV
- 读取历史结果时可扩展

### 10.7 `models`

职责：

- 定义核心数据结构

### 10.8 `error`

职责：

- 定义统一错误类型
- 统一错误转换与传播

### 10.9 `utils`

职责：

- URL 规范化
- 域名过滤
- 时间处理

## 11. 建议目录结构

```text
rust_crawler/
├─ Cargo.toml
├─ README.md
├─ src/
│  ├─ main.rs
│  ├─ lib.rs
│  ├─ cli.rs
│  ├─ config.rs
│  ├─ crawler.rs
│  ├─ fetcher.rs
│  ├─ parser.rs
│  ├─ storage.rs
│  ├─ models.rs
│  ├─ error.rs
│  └─ utils.rs
├─ tests/
│  ├─ parser_tests.rs
│  ├─ storage_tests.rs
│  ├─ utils_tests.rs
│  └─ crawler_tests.rs
└─ output/
   └─ sample_result.json
```

## 12. 核心数据结构草案

```rust
pub struct CrawlerConfig {
    pub start_urls: Vec<String>,
    pub max_depth: usize,
    pub max_pages: usize,
    pub worker_count: usize,
    pub output_path: String,
    pub same_domain_only: bool,
}

pub struct CrawlTask {
    pub url: String,
    pub depth: usize,
}

pub struct PageData {
    pub url: String,
    pub title: Option<String>,
    pub status_code: u16,
    pub links: Vec<String>,
    pub content_length: usize,
}

pub struct CrawlStats {
    pub visited_pages: usize,
    pub success_pages: usize,
    pub failed_pages: usize,
    pub discovered_links: usize,
}
```

后续实现时可根据实际需求调整字段设计。

## 13. 错误处理方案

本项目中错误处理会作为重点工程规范体现。

### 13.1 处理原则

- 所有可能失败的操作使用 `Result`
- 使用 `?` 传播错误
- 避免到处使用 `unwrap` 和 `expect`
- 在合适的位置将底层错误转换为统一应用错误

### 13.2 主要错误来源

- URL 非法
- 请求超时
- 网络连接失败
- 页面解析失败
- 文件写入失败
- 参数配置错误

### 13.3 统一错误类型

建议使用 `thiserror` 定义 `AppError`，统一封装：

- `NetworkError`
- `ParseError`
- `IoError`
- `ConfigError`
- `StorageError`

## 14. 测试规划

课程要求至少包含单元测试与关键功能测试，本项目测试方案如下。

### 14.1 单元测试

重点测试：

- URL 规范化函数
- 域名过滤函数
- HTML 链接提取函数
- 页面标题提取函数
- 配置校验逻辑
- 结果导出逻辑

### 14.2 集成测试

重点测试：

- 给定本地 HTML 样本，是否能正确提取链接
- 小规模抓取流程是否能闭环执行
- 多线程调度下页面数限制是否生效
- 去重逻辑是否正确

### 14.3 测试实现建议

为避免依赖真实外网环境，建议：

- 多使用本地 HTML 测试样本
- 对网络层进行适度封装，便于替换为 mock 数据
- 关键逻辑尽量拆成纯函数，便于单元测试

## 15. 工程规范

提交前必须通过以下命令：

```bash
cargo fmt
cargo clippy -- -D warnings
cargo test
```

代码规范建议：

- 模块边界清晰
- 重要公开函数写文档注释
- 避免过长函数
- 避免重复代码
- 合理拆分类型和职责

## 16. README 规划

最终 README 建议包含以下内容：

1. 项目简介
2. 功能说明
3. 技术选型
4. 目录结构
5. 编译运行方法
6. 命令行参数说明
7. 示例运行结果
8. 测试方法
9. 已知限制
10. 后续优化方向

## 17. 项目开发里程碑

### 第一阶段：需求定型与项目初始化

- 创建 Cargo 项目
- 添加依赖
- 搭建模块骨架
- 编写 README 初稿

### 第二阶段：单线程抓取闭环

- 完成配置解析
- 完成网页请求
- 完成 HTML 解析
- 完成基本抓取流程

### 第三阶段：多线程改造

- 引入共享任务队列
- 引入访问去重集合
- 完成线程调度与统计

### 第四阶段：结果导出与测试

- 导出 JSON / CSV
- 补齐单元测试
- 补齐关键集成测试

### 第五阶段：规范化与文档收尾

- 通过 `fmt`
- 通过 `clippy`
- 完善 README
- 准备课程提交说明

## 18. 预计代码规模

预计代码量控制如下：

- `src/` 约 1200～2200 行
- `tests/` 约 300～700 行

总有效 Rust 代码量预计：

`1500～2900 行`

符合课程推荐规模。

## 19. 项目亮点总结

本项目完成后，能够较好体现：

- Rust 基础语法与类型系统
- 所有权与借用
- `struct`、`enum`、`trait`
- 泛型与必要时的生命周期
- 多线程并发编程
- 统一错误处理
- 模块化工程设计
- 测试与文档能力

## 20. 风险与应对

### 风险 1：真实网站结构复杂，调试成本高

应对方式：

- 前期以结构简单的网站或本地样本页面测试
- 先保证核心流程稳定，再扩大抓取范围

### 风险 2：多线程共享状态容易出错

应对方式：

- 先做单线程版本
- 再逐步引入 `Arc` 和 `Mutex`
- 关键共享逻辑先写测试再集成

### 风险 3：网络环境不稳定影响演示

应对方式：

- 准备本地 HTML 测试样本
- 提供固定演示站点
- 输出明确错误信息

## 21. 结论

“多线程网页爬虫程序”是一个功能明确、技术点丰富、符合课程考核要求的 Rust 结课项目。该项目既能体现 Rust 的系统编程优势，也能展示并发、错误处理、工程化和测试能力。

后续开发建议坚持以下路线：

1. 先做最小可运行版本
2. 再加入多线程并发
3. 最后补齐测试、文档和工程规范

---

## 22. 下一步实施建议

如果按这份规划继续推进，下一步建议直接开始项目初始化，创建：

- `Cargo.toml`
- `src/main.rs`
- `src/lib.rs`
- 各模块骨架文件
- `README.md`
- `tests/` 测试目录

这样后面我们就可以严格按规划实现，不会边做边乱。
